# foodbank-donor-volunteer-crm
A self contained Excel project that cleans a messy donor export and volunteer sign up log, then turns it into a donor engagement dashboard for a food bank. Built using formulas only, no hardcoded values, on top of a real anonymized fundraising dataset relabeled for a nonprofit context.
